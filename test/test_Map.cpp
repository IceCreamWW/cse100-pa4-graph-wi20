#include <gtest/gtest.h>
#include "Map.hpp"

using namespace std;
using namespace testing;

// TODO: add tests for map graph
TEST(ActorGraphTest, SIMPLE_TEST) {
    /*
    ActorGraph* graph = new ActorGraph();
    const char* graphFileName = "data/pathfinder1_graph.tsv";
    graph->buildGraphFromFile(graphFileName);

    string actorFrom = "Kyra Sedgwick";
    string actorTo = "Frank Grillo";

    string shortestPath = "";
    graph->BFS(actorFrom, actorTo, shortestPath);
    ASSERT_EQ("(Kyra Sedgwick)--[Big Sky#@2015]-->(Frank Grillo)", shortestPath);
     */
    ASSERT_EQ(1,1);
}
